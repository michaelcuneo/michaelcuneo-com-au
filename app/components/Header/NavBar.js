import styled from 'styled-components';

export default styled.div`
  display: inline-flex;
  min-width: 100vw;
  align-items: center;
  align-content: center;
  justify-content: space-between;
  -webkit-box-shadow: 0px 2px 35px 2px rgba(0, 0, 0, 0.35);
  -moz-box-shadow: 0px 2px 35px 2px rgba(0, 0, 0, 0.35);
  box-shadow: 0px 2px 35px 2px rgba(0, 0, 0, 0.35);
`;
